{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "config:best-practices",
    ":semanticCommitsDisabled",
    ":automergeStableNonMajor",
    ":automergePatch",
    ":automergeDigest",
    "group:allDigest"
  ],
  "timezone": "Europe/Berlin",
  "schedule": [
    "every weekday",
    "on saturday"
  ],

  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": ["(^|/)versions/unbound\\.txt$"],
      "matchStrings": [
        "(?<currentValue>[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "datasourceTemplate": "github-tags",
      "packageNameTemplate": "NLnetLabs/unbound",
      "extractVersionTemplate": "^release-(?<version>[0-9]+\\.[0-9]+\\.[0-9]+)$",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "fileMatch": ["(^|/)versions/openssl-buildenv\\.txt$"],
      "matchStrings": [
        "OPENSSL_BUILDENV_VERSION=(?<currentValue>.*)"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "openssl-buildenv",
      "extractVersionTemplate": "^v(?<version>.*)$",
      "versioningTemplate": "loose"
    },
    {
      "customType": "regex",
      "fileMatch": ["(^|/)versions/openssl-quic-buildenv\\.txt$"],
      "matchStrings": [
        "OPENSSL_QUIC_BUILDENV_VERSION=(?<currentValue>.*)"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "openssl-buildenv",
      "extractVersionTemplate": "^v(?<version>.*)$",
      "versioningTemplate": "loose"
    }
  ],

  "packageRules": [
    {
      "matchManagers": ["dockerfile"],
      "enabled": false
    },
    {
      "matchManagers": ["github-actions"],
      "pinDigests": true,
      "automerge": true
    }
  ]
}
